.row.headline.admin
  .col-md-6.col-md-offset-3
    - if params[:looking_for_work] == "true"
      %h2 Editors Looking for Work
    - elsif params[:looking_for_work] == "false"
      %h2 Editors Not Looking for Work
    - else
      %h2 All Editors

.row
  .col-md-6.col-md-offset-3
    .search-buttons
      = form_tag admin_index_path, method: "get", class: "form-inline"
      .form-group
        = label_tag :query, "Search for:"
        = select_tag :query, options_for_select(admin_search_options), class: "form-control"
      = submit_tag "Search", class: "btn btn-default"


.row
  .col-md-10.col-md-offset-1
    - if @editors.empty?
      .no-results
        %h5 No results found.
    - else
      %table.table
        %tr
          %th Name
          %th Email
          %th Looking for Work?
          %th Editor Classification
          %th
          %th
        - @editors.each do |editor|
          %tr{ class: "#{ looking_for_work_class(editor) }"}
            %td
              = editor.full_name
            %td
              = editor.email
            %td
              - if editor.looking_for_work?
                YES, I WANT MORE WORK
              - else
                NO, LEAVE ME ALONE
            %td
              = editor.pretty_classification
            %td
              = link_to "Edit", edit_user_path(editor), class: "btn btn-default"
            %td
              = link_to "Delete Editor", user_path(editor), method: :delete,  data: { confirm: 'Are you sure?' }, class: "btn btn-danger"
